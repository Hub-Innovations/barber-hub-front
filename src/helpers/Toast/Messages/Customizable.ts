import { ToastPosition } from '@chakra-ui/react';

export const successCustomizableToast = {
  duration: 5000,
  position: 'top-right' as ToastPosition,
  isClosable: true,
};

export const erroCustomizableToast = {
  duration: 5000,
  position: 'top-right' as ToastPosition,
  isClosable: true,
};
